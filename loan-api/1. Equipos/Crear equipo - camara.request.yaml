$kind: http-request
description: ""
url: "{{baseUrl}}/equipment"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "code": "CAM-001",
      "name": "Cámara Canon EOS",
      "stock": 2
    }
scripts:
  - type: afterResponse
    code: |-
      pm.test("Responde 201", function () { pm.response.to.have.status(201); });
      pm.collectionVariables.set("camaraId", pm.response.json().id);
    language: text/javascript
order: 2000
