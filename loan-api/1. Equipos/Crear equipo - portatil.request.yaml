$kind: http-request
description: ""
url: "{{baseUrl}}/equipment"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "code": "PORT-001",
      "name": "Portátil Lenovo ThinkPad",
      "stock": 5
    }
scripts:
  - type: afterResponse
    code: |-
      pm.test("Responde 201", function () { pm.response.to.have.status(201); });
      pm.collectionVariables.set("portatilId", pm.response.json().id);
    language: text/javascript
order: 1000
