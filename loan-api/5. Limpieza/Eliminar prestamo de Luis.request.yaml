$kind: http-request
description: Seguia activo, asi que la unidad vuelve al stock de la camara.
url: "{{baseUrl}}/loans/{{prestamoLuisId}}"
method: DELETE
scripts:
  - type: afterResponse
    code: pm.test("Responde 204", function () { pm.response.to.have.status(204); });
    language: text/javascript
order: 2000
