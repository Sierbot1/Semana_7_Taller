$kind: http-request
description: ""
url: "{{baseUrl}}/students/{{luisId}}"
method: DELETE
scripts:
  - type: afterResponse
    code: pm.test("Responde 204", function () { pm.response.to.have.status(204); });
    language: text/javascript
order: 4000
