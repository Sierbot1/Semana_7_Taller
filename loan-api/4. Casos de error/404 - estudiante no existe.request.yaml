$kind: http-request
description: ""
url: "{{baseUrl}}/loans"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "studentId": 99999,
      "equipmentId": "{{portatilId}}",
      "quantity": 1
    }
scripts:
  - type: afterResponse
    code: |-
      pm.test("Responde 404", function () { pm.response.to.have.status(404); });
      pm.test("Mensaje de error claro", function () { pm.expect(pm.response.json().error).to.be.a("string").and.not.empty; });
    language: text/javascript
order: 7000
